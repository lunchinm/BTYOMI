# BTYOMI - AI 智能助手规划

## 一、功能定位

新建独立模块 **aiAssistant**，打造一个**自然语言为主、五问式为辅**的 AI 智能选车助手。同时建立**用户行为画像**，记录用户在各模块的浏览、收藏、点击等行为，用于优化推荐结果。

### 核心价值
- **自然语言优先**：用户直接用口语描述需求，AI 智能理解
- **五问式辅助**：当用户需求不明确时，自动切换为五问式引导
- **行为驱动推荐**：用户在全 App 的行为数据影响推荐结果
- **独立模块**：不依赖 buyingCar/explore 等现有模块，可独立演进

---

## 二、模块结构

### 2.1 独立模块定位

```
BTYOMI/
├── features/
│   ├── buyingCar/        # 已有：购车服务模块
│   ├── explore/          # 已有：发现探索模块
│   ├── mine/             # 已有：个人中心模块
│   ├── service/          # 已有：服务中心模块
│   ├── shoppingMall/     # 已有：商城模块
│   └── aiAssistant/      # ✨ 新增：AI智能助手模块（独立）
└── common/
    └── src/main/ets/
        ├── model/
        │   └── UserBehaviorModel.ets   # ✨ 用户行为模型（共享）
        └── utils/
            └── BehaviorTracker.ets     # ✨ 行为埋点工具（共享）
```

### 2.2 模块职责

| 模块 | 职责 | 与 AI 助手的关系 |
|------|------|-----------------|
| **aiAssistant** | 聊天界面、对话逻辑、推荐展示 | 核心模块，独立存在 |
| **buyingCar** | 车型列表、详情页 | 提供数据，行为数据回传给 AI |
| **explore** | 活动、资讯 | 行为数据回传给 AI |
| **shoppingMall** | 商品、商城 | 行为数据回传给 AI |
| **mine** | 收藏、历史记录 | 行为数据回传给 AI |
| **common** | 共享工具、模型 | 行为埋点工具、用户画像模型 |

---

## 三、参考项目

本项目的聊天界面设计参考自 [awesome-llm-apps](https://github.com/Shubhamsaboo/awesome-llm-apps) 仓库中的以下项目：

| 项目 | 借鉴点 |
|------|--------|
| **Generative UI Starter Project** | 聊天 + 画布双栏布局、共享状态 |
| **AI Financial Coach Agent** | 顾问式对话流程、渐进式信息收集 |
| **AI Travel Agent** | 简单实用的聊天式交互 |

参考项目代码位于同级目录：`../reference-projects/`

---

## 四、运行环境与验证说明

### 4.1 开发环境

- **IDE**: DevEco Studio
- **系统**: HarmonyOS Next
- **语言**: ArkTS

### 4.2 模拟器可验证功能（DevEco Simulator）

以下功能在 DevEco 模拟器中可以完整开发和验证：

| 功能模块 | 验证程度 | 说明 |
|----------|----------|------|
| **聊天界面 UI** | ✅ 完全可验证 | 消息气泡、输入框、列表滚动等纯 UI 组件 |
| **消息类型展示** | ✅ 完全可验证 | 文本消息、选项消息、车型卡片、对比表格 |
| **快捷问题按钮** | ✅ 完全可验证 | 按钮点击、消息发送交互 |
| **五问式引导** | ✅ 完全可验证 | 选项选择、步骤切换、状态管理 |
| **本地推荐算法** | ✅ 完全可验证 | 五维评分算法、高斯分布、降级策略 |
| **状态管理** | ✅ 完全可验证 | @State / @Provide / @Consume 状态流转 |
| **用户画像数据结构** | ✅ 完全可验证 | 数据模型定义、状态更新逻辑 |
| **本地存储（Preferences）** | ✅ 基本可验证 | PreferencesUtil 读写功能在模拟器可用 |
| **页面路由跳转** | ✅ 基本可验证 | Navigation.pushPath / pop 路由跳转 |
| **动画效果** | ✅ 基本可验证 | 加载动画、消息入场动画 |

### 4.3 模拟器无法验证（需真机）

以下功能代码可以实现，但在模拟器中无法完整验证，需要真机运行：

| 功能模块 | 无法验证原因 | 真机验证点 |
|----------|-------------|-----------|
| **火山方舟 AI API** | 模拟器网络受限/证书问题 | HTTP 请求、流式响应、API 鉴权 |
| **语音输入** | 模拟器无麦克风硬件 | 语音识别、语音转文字 |
| **图片上传/拍照** | 模拟器无摄像头/相册受限 | 拍照识别车型、相册图片上传 |
| **分享功能** | 模拟器无系统分享 | 系统分享面板、分享到微信等 |
| **定位服务** | 模拟器定位固定 | 附近充电站、位置-based推荐 |
| **推送通知** | 模拟器推送通道不可用 | 推荐结果推送、消息提醒 |
| **生物识别** | 模拟器无指纹/人脸硬件 | 指纹登录、支付验证 |
| **性能表现** | 模拟器性能与真机差异大 | 列表流畅度、内存占用、冷启动时间 |
| **手势导航** | 模拟器手势模拟有限 | 侧滑返回、多指触控 |
| **权限弹窗** | 模拟器权限行为不同 | 相机/麦克风/定位权限申请流程 |
| **横屏/分屏适配** | 模拟器尺寸固定 | 多设备形态适配、折叠屏适配 |

### 4.4 开发建议

**第一阶段（模拟器开发）**：
- 优先完成所有纯 UI 和本地逻辑
- AI 对接使用 Mock 数据模拟响应
- 确保界面交互、状态管理、本地算法正确

**第二阶段（真机验证）**：
- 接入真实火山方舟 API
- 验证语音输入和图片上传
- 测试性能和流畅度
- 验证系统级能力（分享、推送等）

---

## 五、界面设计

### 5.1 整体布局

```
┌─────────────────────────────────────────────────┐
│  🚗 AI 选车顾问                          ─ □ ✕ │
├─────────────────────────────────────────────────┤
│                                                 │
│   [欢迎消息]                                     │
│   你好！我是你的AI选车顾问，告诉我你的需求，     │
│   我来帮你找到最适合的车型 🚗                    │
│                                                 │
│   [快捷问题按钮]                                 │
│   ┌──────────┬──────────┬──────────┐            │
│   │15万纯电SUV│ 续航最长 │ 家庭用车 │            │
│   └──────────┴──────────┴──────────┘            │
│                                                 │
│   [用户消息]              [用户头像]            │
│                          我想要20万左右的SUV     │
│                                                 │
│   [AI消息]    [AI头像]                          │
│               好的，20万左右的SUV。请问你更看    │
│               重哪个方面呢？                     │
│               • 续航里程                         │
│               • 空间大小                         │
│               • 智能驾驶                         │
│               • 都重要，综合推荐                 │
│                                                 │
│   [AI消息]    [AI头像]                          │
│               为你推荐以下3款车型：              │
│               ┌─────────────────────────────┐   │
│               │  🚙 比亚迪宋PLUS EV          │   │
│               │  💰 18.98万起  ⚡ 520km     │   │
│               │  ⭐ 匹配度 95%               │   │
│               │  [查看详情]  [加入对比]      │   │
│               └─────────────────────────────┘   │
│               ┌─────────────────────────────┐   │
│               │  🚙 特斯拉Model Y            │   │
│               │  💰 24.99万起  ⚡ 554km     │   │
│               │  ⭐ 匹配度 88%               │   │
│               │  [查看详情]  [加入对比]      │   │
│               └─────────────────────────────┘   │
│                                                 │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────┐  📎  🎙│
│  │  输入你的需求...                     │       │
│  └─────────────────────────────────────┘       │
│                                      [发送]     │
└─────────────────────────────────────────────────┘
```

### 5.2 组件说明

| 组件 | 位置 | 功能 |
|------|------|------|
| **顶部导航栏** | 顶部 | 标题 + 返回按钮 |
| **消息列表** | 中间 | 滚动展示对话历史 |
| **快捷问题** | 消息列表顶部 | 3-4个常用问题按钮 |
| **用户气泡** | 右侧 | 用户发送的消息 |
| **AI 气泡** | 左侧 | AI 回复的消息 |
| **车型卡片** | AI 气泡内 | 推荐的车型信息卡片 |
| **输入框** | 底部 | 文本输入 + 发送按钮 |
| **附件按钮** | 输入框左侧 | 上传图片/配置需求 |
| **语音按钮** | 输入框右侧 | 语音输入 |

---

## 六、消息类型

### 6.1 文本消息

```
┌─────────────┐
│ 纯文本内容    │
└─────────────┘
```

### 6.2 选项消息（引导式提问）

```
┌──────────────────┐
│ AI 问题文本       │
│                  │
│ • 选项A          │
│ • 选项B          │
│ • 选项C          │
└──────────────────┘
```

用户点击选项后，选项变为灰色不可点击，同时将选项内容作为用户消息发送。

### 6.3 车型卡片消息

```
┌───────────────────────────────┐
│  [车型图片]                    │
│  品牌 + 车型名称                │
│  价格  ·  续航  ·  动力类型     │
│  ⭐ 匹配度 95%                 │
│  [查看详情]  [加入对比]         │
└───────────────────────────────┘
```

### 6.4 对比表格消息

```
┌────────────────────────────────────┐
│         车型A    │  车型B           │
├─────────────────┼──────────────────┤
│ 价格    xxx万   │  xxx万           │
│ 续航    xxxkm   │  xxxkm           │
│ 动力    纯电    │  增程式          │
│ ...             │  ...             │
└────────────────────────────────────┘
```

### 6.5 加载消息

```
[AI头像]  ● ● ●   （三个点跳动动画）
```

---

## 七、对话流程设计

### 7.1 核心原则：自然语言为主，五问式为辅

```
用户输入
    │
    ▼
AI 分析需求完整度
    │
    ├─ 信息完整度 ≥ 70% → 直接推荐（自然语言路径）
    │
    └─ 信息完整度 < 70% → 追问缺失的关键信息
              │
              ├─ 用户选择：继续用文字回答 → 自然语言路径
              │
              └─ 用户选择：用选项回答 → 切换五问式引导
```

**自然语言为主**：用户可以自由描述需求，AI 通过语义理解提取关键信息。

**五问式为辅**：当用户需求模糊或表达不清时，提供结构化的选项引导，降低用户思考成本。

#### 7.1.1 信息完整度量化标准

信息完整度采用**规则匹配 + AI 语义理解**双引擎评估：

| 维度 | 权重 | 判定标准 |
|------|------|----------|
| **预算范围** | 25% | 明确提到价格区间（如"15万"、"20万左右"）|
| **续航需求** | 20% | 明确提到续航里程（如"500km"、"续航越长越好"）|
| **车型偏好** | 20% | 明确提到车型（如"SUV"、"轿车"、"MPV"）|
| **动力类型** | 15% | 明确提到动力（如"纯电"、"混动"、"增程"）|
| **功能偏好** | 20% | 明确提到功能点（如"空间大"、"智能驾驶"、"性价比"）|

**计算方式**：
```
完整度 = Σ(各维度得分 × 权重)
```
- 每个维度：提到得满分，未提到得0分
- AI 语义理解提取的信息可直接计入对应维度

**兜底策略**：
- 如果 AI 判断失误（高估完整度）：推荐结果质量低，用户会主动追问，系统捕获追问后重新评估
- 如果 AI 判断失误（低估完整度）：多追问1-2个问题，用户体验影响较小
- 本地规则引擎降级时：使用严格模式（必须明确提到3个以上维度才判定≥70%）

### 7.2 开场流程

```
用户进入页面
    │
    ▼
AI 发送欢迎消息 + 快捷问题按钮
    │
    ├─ 用户点击快捷问题 → 直接进入对应话题
    │
    └─ 用户手动输入 → 根据内容智能回应
```

### 7.3 推荐主流程

```
用户描述需求
    │
    ▼
AI 理解需求，提取关键信息（预算/续航/车型/动力/偏好）
    │
    ▼
判断信息完整度
    │
    ├─ ✅ 信息足够 → 直接推荐车型（车型卡片）
    │
    └─ ❌ 信息不足 → 追问缺失的 1-2 个关键问题
              │
              ├─ 纯文本追问 + 选项按钮
              │
              └─ 用户回答后继续推荐
```

### 7.4 五问式辅助引导

当用户需求不明确时，AI 逐步提问（最多5步），每步提供选项按钮：

| 步骤 | 问题 | 选项示例 |
|------|------|----------|
| Q1 | 预算范围 | 15万以下 / 15-25万 / 25-35万 / 35万以上 |
| Q2 | 续航需求 | 400km以下 / 400-500km / 500-600km / 600km以上 |
| Q3 | 车型偏好 | 轿车 / SUV / MPV / 无所谓 |
| Q4 | 动力类型 | 纯电动 / 增程式 / 混合动力 / 无所谓 |
| Q5 | 其他偏好 | 空间大 / 智能化 / 性价比 / 品牌 |

> 用户随时可以**跳过五问，直接用自然语言描述**，AI 会自动切换回自然语言模式。

### 7.5 后续追问流程

```
推荐车型后
    │
    ├─ "再便宜点的" → 调整价格区间，重新推荐
    ├─ "还有别的吗" → 展示更多候选车型
    ├─ "对比一下" → 生成对比表格
    ├─ "详细说说" → 展示车型详情
    ├─ "我想要SUV" → 补充需求，重新推荐
    └─ "重新来" → 清空对话，重新开始
```

### 7.6 异常流程处理

```
AI 请求发起
    │
    ├─ 网络异常 → 显示 ERROR 消息（可重试）
    │               │
    │               ├─ 用户点击重试 → 重新发起请求
    │               └─ 用户放弃 → 降级为本地规则引擎
    │
    ├─ API 超时（>10s）→ 显示 ERROR 消息（可重试）
    │               │
    │               └─ 自动触发降级
    │
    ├─ API 返回错误 → 解析错误码，显示对应提示
    │               │
    │               ├─ 4xx（客户端错误）→ 不可重试，提示用户修正输入
    │               └─ 5xx（服务端错误）→ 可重试，自动降级
    │
    └─ 解析失败 → 显示 ERROR 消息（可重试）
                    │
                    └─ 降级为本地规则引擎
```

**降级触发条件**：
| 触发条件 | 是否自动降级 | UI 表现 |
|----------|-------------|---------|
| 网络不可用 | 是 | 显示"网络异常，已切换到本地模式" |
| API 超时（>10s） | 是 | 显示"连接超时，已切换到本地模式" |
| API 返回 5xx 错误 | 是 | 显示"服务繁忙，已切换到本地模式" |
| 连续重试失败（≥3次） | 是 | 显示"连接失败，已切换到本地模式" |
| 用户手动选择"离线模式" | 是 | 显示"已切换到本地模式" |

**降级后的 UI 表现**：
- 底部状态栏显示"本地模式"标识
- AI 回复质量可能下降（基于规则匹配）
- 推荐结果仅基于本地数据库
- 不再调用火山方舟 API

**恢复机制**：
- 每隔 30 秒自动检测网络状态
- 网络恢复后自动切换回 AI 模式
- 显示"已恢复 AI 模式"提示
- 用户可手动点击"重新连接"按钮

---

## 八、技术实现方案

### 8.1 数据结构

```typescript
// 消息类型
enum MessageType {
  TEXT = 'text',           // 纯文本
  OPTIONS = 'options',     // 选项列表
  CAR_CARD = 'car_card',   // 车型卡片
  COMPARISON = 'comparison', // 对比表格
  LOADING = 'loading',     // 加载中
  ERROR = 'error'          // 错误消息（API超时、网络异常、服务端报错）
}

// 错误状态
interface ErrorState {
  code: string;           // 错误码（NETWORK_ERROR / API_TIMEOUT / API_ERROR / PARSE_ERROR）
  message: string;        // 用户可读的错误提示
  retryable: boolean;     // 是否可重试
  timestamp: number;
}

// 单条消息
interface ChatMessage {
  id: string;
  role: 'user' | 'assistant';
  type: MessageType;
  content: string;                 // 文本内容
  options?: string[];              // 选项列表
  cars?: CarInfo[];                // 车型卡片数据
  comparison?: ComparisonData;     // 对比数据
  timestamp: number;
  errorCode?: string;              // 错误码（仅ERROR类型有效）
  retryable?: boolean;             // 是否可重试（仅ERROR类型有效）
  metadata?: Record<string, string>; // 扩展元数据（如token消耗、模型名称）
  isStreaming?: boolean;           // 是否正在流式输出
}

// 对话状态
interface ChatState {
  messages: ChatMessage[];
  isLoading: boolean;
  isStreaming: boolean;            // 是否正在流式输出（与isLoading不同语义）
  currentContext: UserPreferences;  // 当前对话收集的需求
  sessionId: string;               // 会话ID，用于多轮对话上下文管理
  error: ErrorState | null;        // 全局错误状态
}
```

### 8.2 核心页面文件结构

```
features/aiAssistant/src/main/ets/
├── pages/
│   └── AIChatPage.ets          # 聊天主页面
├── components/
│   ├── ChatMessageBubble.ets   # 消息气泡组件
│   ├── CarCardMessage.ets      # 车型卡片消息
│   ├── OptionsMessage.ets      # 选项消息（五问式引导）
│   ├── ComparisonMessage.ets   # 对比表格消息
│   ├── QuickQuestions.ets      # 快捷问题组件
│   └── ChatInputBar.ets        # 底部输入栏
├── model/
│   └── ChatModel.ets           # 聊天数据模型
└── service/
    └── ChatService.ets         # 聊天逻辑服务
```

### 8.3 共享模块（common）

```
common/src/main/ets/
├── model/
│   └── UserBehaviorModel.ets   # 用户行为画像数据模型
└── utils/
    └── BehaviorTracker.ets     # 行为埋点工具类
```

### 8.4 状态管理

使用 `@State` 和 `@Provide`/`@Consume` 管理对话状态：

- `messages`：消息列表（核心状态）
- `isLoading`：AI 是否正在回复
- `inputText`：输入框内容
- `userPreferences`：当前收集到的用户需求
- `userProfile`：用户行为画像（全局共享）

### 8.5 AI 对接

- **主方案**：火山方舟 API（与现有 AICarRecommendService 复用）
- **降级方案**：本地规则引擎 + 关键词匹配
- **多轮对话**：将历史消息拼接为 context 传入 API
- **画像增强**：将用户行为画像作为 system prompt 的一部分传入

#### 8.5.1 Prompt 工程设计

**System Prompt 结构**：
```
你是一个专业的AI选车顾问，名叫"车顾问"。

【角色设定】
- 专业、耐心、友好，用通俗易懂的语言回答
- 熟悉中国汽车市场，了解主流车型参数
- 优先推荐市场口碑好、性价比高的车型

【能力边界】
- 可以回答关于车型推荐、参数对比、购车建议的问题
- 可以帮助用户分析不同车型的优缺点
- 不能回答与汽车无关的问题
- 不能提供财务建议或贷款方案
- 如果不确定答案，明确告知用户

【输出格式约束】
- 推荐车型时，必须从数据库中选择真实存在的车型
- 输出车型参数必须准确，不要虚构数据
- 当需要追问用户时，提供3-4个选项供选择
- 推荐结果以结构化卡片形式呈现

【用户画像】
${userProfile}

【对话历史】
${historyMessages}

【当前用户输入】
${userInput}
```

**Token 限制策略**：
- 火山方舟 doubao-seed-2-0-mini 上下文窗口：4096 tokens
- 预留 1024 tokens 给 AI 回复
- 历史消息保留最近 20 轮（约 3000 tokens）

**上下文截断策略**：
- 超过 token 限制时，优先保留最近的对话
- 对更早的对话进行摘要压缩（取每轮的核心意图）
- 极端情况下只保留最近 5 轮对话

**幻觉防护**：
- AI 返回的车型名称必须在本地数据库中存在
- 参数数据（价格、续航等）与本地数据库交叉验证
- 如果 AI 返回了不在数据库中的车型，提示用户"该车型暂未收录"
- 对 AI 返回的 JSON 格式进行严格校验，格式错误则降级为文本

### 8.6 用户行为画像系统

#### 8.6.1 行为数据采集

| 行为类型 | 触发场景 | 权重 |
|----------|----------|------|
| **收藏** | 点击车型收藏按钮 | ⭐⭐⭐⭐⭐ 高权重 |
| **详情浏览** | 进入车型详情页 | ⭐⭐⭐ 中权重 |
| **停留时长** | 在详情页停留时间 | ⭐⭐⭐ 中权重 |
| **点击推荐** | 点击 AI 推荐的车型卡片 | ⭐⭐⭐⭐ 高权重 |
| **搜索关键词** | 搜索栏输入关键词 | ⭐⭐⭐⭐ 高权重 |
| **分享** | 分享车型给他人 | ⭐⭐⭐⭐⭐ 高权重 |
| **浏览列表** | 滚动浏览车型列表 | ⭐ 低权重 |
| **对话关键词** | 聊天中提到的偏好词 | ⭐⭐⭐⭐ 高权重 |

#### 8.6.2 画像维度

```typescript
interface UserBehaviorProfile {
  // 价格偏好
  priceRange: {
    preferred: string;      // 偏好价格区间
    confidence: number;     // 置信度 0-1
  };
  // 车型偏好
  bodyType: {
    preferred: string;      // 偏好车型（SUV/轿车/MPV）
    confidence: number;
  };
  // 动力偏好
  powerType: {
    preferred: string;      // 偏好动力（纯电/增程/混动）
    confidence: number;
  };
  // 品牌偏好
  brandPreference: {
    brands: string[];       // 关注的品牌列表
    scores: Record<string, number>; // 各品牌分数
  };
  // 功能偏好
  featurePreference: {
    features: string[];     // 关注的功能（智能驾驶/空间/续航等）
    scores: Record<string, number>;
  };
  // 行为统计
  stats: {
    totalViews: number;     // 总浏览次数
    totalFavorites: number; // 总收藏数
    lastActiveTime: number; // 最后活跃时间
  };
}
```

#### 8.6.3 行为埋点触发点

需要在以下页面添加埋点（每个页面的事件远不止一处）：

| 模块 | 页面 | 埋点事件 | 详细说明 |
|------|------|----------|----------|
| **buyingCar** | BuyingCarPage | 列表浏览曝光 | `onScroll` 触发，节流 200ms，记录可见车型 ID |
| **buyingCar** | BuyingCarPage | 点击车型卡片 | 记录车型 ID、位置索引 |
| **buyingCar** | BuyingCarPage | 搜索关键词 | 记录搜索内容、时间 |
| **buyingCar** | BuyingCarDetailPage | 进入详情页 | 记录车型 ID、来源页面 |
| **buyingCar** | BuyingCarDetailPage | 收藏（添加） | 记录车型 ID、时间 |
| **buyingCar** | BuyingCarDetailPage | 收藏（取消） | 记录车型 ID、时间 |
| **buyingCar** | BuyingCarDetailPage | 分享 | 记录车型 ID、分享渠道 |
| **buyingCar** | BuyingCarDetailPage | 停留时长 | 计时器，页面离开时记录 |
| **buyingCar** | BuyingCarDetailPage | 加入对比 | 记录车型 ID |
| **explore** | ExplorePage | 内容曝光 | `onScroll` 触发，记录可见内容 ID |
| **explore** | ExplorePage | 点击活动 | 记录活动 ID |
| **explore** | ExplorePage | 点击资讯 | 记录资讯 ID |
| **shoppingMall** | ShoppingMallPage | 商品曝光 | `onScroll` 触发，记录可见商品 ID |
| **shoppingMall** | ShoppingMallPage | 商品收藏（添加） | 记录商品 ID |
| **shoppingMall** | ShoppingMallPage | 商品收藏（取消） | 记录商品 ID |
| **shoppingMall** | ShoppingMallPage | 商品点击 | 记录商品 ID |
| **mine** | MinePage | 查看收藏列表 | 记录时间、列表长度 |
| **mine** | MinePage | 点击收藏车型 | 记录车型 ID |
| **aiAssistant** | AIChatPage | 对话关键词 | 提取用户消息中的关键词 |
| **aiAssistant** | AIChatPage | 点击推荐卡片 | 记录车型 ID、推荐排名 |
| **aiAssistant** | AIChatPage | 快捷问题点击 | 记录问题内容 |

#### 8.6.3.1 性能考虑

| 事件类型 | 频率 | 处理策略 |
|----------|------|----------|
| **滚动曝光** | 高频（每帧） | 节流 200-300ms，仅记录新增可见元素 |
| **点击事件** | 低频 | 直接记录，无需节流 |
| **停留时长** | 低频 | 页面离开时一次性记录 |
| **对话关键词** | 中频 | 每次用户发送消息时提取 |

#### 8.6.3.2 BehaviorTracker 生命周期管理

- **单例模式**：全局唯一实例，避免重复初始化
- **初始化时机**：App 启动时（`Application.onCreate`）
- **内存管理**：弱引用持有，避免内存泄漏
- **持久化时机**：每次 track 后异步写入 Preferences（批量写入优化）

#### 8.6.3.3 buyingCar 模块改造风险评估

| 风险点 | 影响范围 | 风险等级 | 缓解措施 |
|--------|----------|----------|----------|
| 修改现有页面代码 | buyingCar 模块 | 高 | 先写单元测试，再修改代码 |
| 埋点逻辑引入性能问题 | 列表滚动流畅度 | 中 | 节流 + 异步写入 |
| 状态管理冲突 | 收藏状态同步 | 中 | 使用 @Provide/@Consume 共享状态 |
| 回归测试覆盖不全 | 原有功能正常运行 | 中 | 手工测试关键路径 |

#### 8.6.4 画像影响推荐的方式

```
推荐算法输入
    │
    ├─ 用户当前需求（对话中提取）
    │
    └─ 用户行为画像（历史行为累计）
         │
         ├─ 提升偏好品牌/车型的权重
         ├─ 降低不感兴趣类型的权重
         └─ 发现潜在兴趣（浏览过但未收藏的类型）
```

**推荐公式**（示意）：

```
最终得分 = 基础匹配分 × 0.6
         + 画像匹配分 × 0.3
         + 热门度分 × 0.1
```

#### 8.6.5 数据存储

- **本地存储**：使用 `PreferencesUtil` 存储用户画像数据
- **持久化**：App 退出后数据保留
- **过期策略**：30 天前的行为数据权重衰减

#### 8.6.6 冷启动策略

**问题**：新用户第一次使用时，画像为空，`画像匹配分 × 0.3` 这一项形同虚设。

**渐进式画像构建策略**：
```
用户首次使用
    │
    ├─ 前 3 次对话：画像权重 = 0%（纯当前需求驱动）
    │               推荐公式：基础匹配分 × 0.9 + 热门度分 × 0.1
    │
    ├─ 第 4-10 次对话：画像权重逐步提升至 15%
    │               推荐公式：基础匹配分 × 0.8 + 画像匹配分 × 0.15 + 热门度分 × 0.05
    │
    ├─ 第 11-20 次对话：画像权重提升至 25%
    │               推荐公式：基础匹配分 × 0.65 + 画像匹配分 × 0.25 + 热门度分 × 0.1
    │
    └─ 20 次对话后：画像权重稳定在 30%
                    推荐公式：基础匹配分 × 0.6 + 画像匹配分 × 0.3 + 热门度分 × 0.1
```

**新用户引导**：
- 首次进入时，主动询问 1-2 个核心偏好问题（预算、车型）
- 快捷问题按钮展示热门搜索词
- 推荐结果优先展示市场热门车型

#### 8.6.7 隐私合规

**隐私声明**：
- 用户首次进入 App 时，弹出隐私政策确认弹窗
- 告知用户行为数据会被收集用于个性化推荐
- 明确说明数据仅存储在本地，不会上传到云端（除发送给火山方舟 API 的对话内容）

**用户控制权**：
- 用户可在"我的"页面查看行为画像数据
- 用户可选择"清除画像数据"重置所有行为记录
- 用户可选择"关闭个性化推荐"，此时画像权重降为 0

**数据安全**：
- 本地存储的画像数据使用简单加密（Base64 编码 + 混淆）
- 对话历史仅存储在本地 Preferences 中
- 发送到火山方舟 API 的数据包含用户输入和画像信息，需在隐私政策中明确说明

### 8.7 流式响应设计

**需求**：AI 回复逐字显示，提升用户体验。

**数据模型支持**：
- `ChatMessage.isStreaming`: 标识当前消息是否正在流式输出
- `ChatMessage.content`: 逐步增长的文本内容

**流式响应与加载消息的关系**：
- 发起请求时：先显示 LOADING 消息（三个点跳动）
- 收到第一个流式 chunk 时：将 LOADING 消息转换为 TEXT 消息，并设置 `isStreaming: true`
- 流式输出过程中：逐步更新 `content` 字段
- 流式结束时：设置 `isStreaming: false`

**技术实现**：
- 火山方舟 API 设置 `stream: true`，响应格式为 SSE（Server-Sent Events）
- ArkTS 使用 `@ohos.net.http` 的流式读取能力，或使用 WebSocket 模拟
- 每收到一个 chunk，更新 UI 显示（使用 `@State` 触发视图刷新）

---

## 九、现有功能盘点与框架先行策略

### 9.1 当前项目已有功能

| 模块 | 已有功能 | 对应规划位置 |
|------|----------|-------------|
| **buyingCar** | 五问式 AI 选车问卷 | 规划的"五问式为辅"部分已实现 |
| **buyingCar** | 本地五维评分算法（高斯/对数/矩阵） | 规划的"降级方案"已实现 |
| **buyingCar** | 火山方舟 API 对接（豆包 Seed 2.0 Mini） | 规划的"主方案"已实现 |
| **buyingCar** | Q5 自然语言文本输入 | 规划的"自然语言输入"雏形已有 |
| **common** | PreferencesUtil | 规划的"本地存储"工具已存在 |
| **common** | LoggerUtil | 可用于埋点日志 |
| **entry** | Navigation + PagesMap 路由体系 | 规划页面需接入此体系 |
| **全局** | @Provide/@Consume 路由栈管理 | 新模块需遵循同样模式 |

### 9.2 规划中尚未实现（需搭框架）

| 功能 | 优先级 | 框架先行内容 | 后续填充 |
|------|--------|-------------|----------|
| **独立 aiAssistant 模块** | ⭐⭐⭐⭐⭐ | 创建模块骨架、路由注册 | 填充聊天 UI 和逻辑 |
| **聊天界面（消息气泡）** | ⭐⭐⭐⭐⭐ | ChatMessageBubble 组件（支持文本） | 扩展车型卡片、对比表等富消息 |
| **消息类型枚举** | ⭐⭐⭐⭐⭐ | 定义 MessageType 枚举和 ChatMessage 接口 | 新增类型时扩展枚举 |
| **用户行为画像模型** | ⭐⭐⭐⭐ | UserBehaviorProfile 接口 + 空实现 | 后续填充各维度计算逻辑 |
| **BehaviorTracker 埋点工具** | ⭐⭐⭐⭐ | 工具类骨架 + track() 空方法 | 后续接入 Preferences 持久化 |
| **ChatService 聊天服务** | ⭐⭐⭐⭐ | 单例骨架 + sendMessage() 空实现 | 后续接入火山方舟 API |
| **快捷问题组件** | ⭐⭐⭐ | 组件骨架 + 固定示例数据 | 后续动态化 |
| **对话状态管理** | ⭐⭐⭐⭐ | messages / isLoading / userPreferences 状态 | 后续扩展多轮上下文 |

### 9.3 框架先行开发思路

```
搭架子（先让结构跑通）
    │
    ├─ 模块结构：aiAssistant 模块创建、路由注册
    ├─ 数据模型：所有 interface / enum 先定义好
    ├─ 组件骨架：@Component 空壳，只显示占位文本
    ├─ 服务骨架：单例 + 方法签名，返回 Mock 数据
    └─ 埋点工具：方法定义 + 空实现
    │
填充内容（逐步完善）
    │
    ├─ UI 细节：气泡样式、动画、交互
    ├─ 业务逻辑：对话流程、推荐算法
    ├─ 数据持久化：Preferences 存储
    └─ 真机能力：语音、拍照、定位等
```

**优势**：
1. 结构先稳定，避免后续大规模重构
2. 团队协作时可以并行开发（有人搭架子，有人填内容）
3. 模拟器阶段就能验证整体架构是否合理
4. 真机能力可以最后接入，不阻塞主流程

---

## 十、代码规范与注意事项

> 以下内容基于 [CODE_GUIDELINES.md](file:///C:/hongmeng/BTYOMI/CODE_GUIDELINES.md)、FIX_BY_LIH.md、FIX_BY_LIU.md 整理，开发新模块时必须遵守。

### 10.1 装饰器规范

| 规则 | 说明 | 违反后果 |
|------|------|---------|
| **@Entry 不与 @Component 同用** | @Entry 已隐含 @Component | 冗余代码（不报错但不规范） |
| **@Prop 必须有默认值** | `@Prop name: string = ''` | Previewer 报错 |
| **CustomDialogController 用可选** | `controller?: CustomDialogController` | 无法创建默认实例 |
| **@Consume 需要默认值** | `= new NavPathStack()` | 路由栈未传递时崩溃 |
| **@Consume 页面无法直接预览** | 需创建包装组件提供 @Provide | Previewer 无法显示 |

### 10.2 布局与组件规范

| 规则 | 说明 |
|------|------|
| **List 必须显式宽高** | `.width() / .height('100%')` |
| **Column 不支持 .scrollable()** | 需用 Scroll 组件包裹 |
| **Stack 布局注意 zIndex** | 重要交互组件设置 zIndex，避免被遮挡 |
| **点击区域不被遮挡** | 确保上层组件不拦截点击事件 |

### 10.3 路由规范（必须遵守）

1. **新增页面必须注册路由**：在 `NavigationPage.ets` 的 PagesMap 中添加
2. **pushPath 导航的页面必须用 NavDestination 包装**：`.hideTitleBar(true)`
3. **路由栈用 @Provide/@Consume 传递**：`@Consume('pageInfos') pageInfos: NavPathStack = new NavPathStack()`

### 10.4 状态管理规范

1. **选项初始值不与选项值匹配**：用 `-1` 或 `''` 避免自动选中
2. **步骤状态隔离**：进入下一步时重置后续步骤状态，避免污染
3. **选项值唯一化**：不同问题的相同选项用不同 value（如 `q3_any`、`q4_any`）
4. **UI 层与算法层数据转换**：UI 用特殊值，调用算法前转回标准值
5. **aboutToAppear 中重置状态**：pushPath 的页面会挂起保留状态，重新进入需重置

### 10.5 语言限制（ArkTS）

| 限制 | 正确写法 |
|------|---------|
| **禁止对象展开运算符** | 禁止 `...obj`，用显式属性赋值 |
| **禁止 any / unknown 类型** | 使用具体类型或 `as` 断言 |
| **禁止 `is` 类型守卫** | 用 `instanceof` 或 `as` 断言 |
| **@Builder 只能有 UI 语法** | 不能包含赋值、状态修改 |

### 10.6 推荐算法规范

1. **必须有降级策略**：API 失败 → 本地算法
2. **必须有兜底逻辑**：确保至少返回 N 个结果
3. **高优先级结果优先**：如 90 分以上优先，不足则补齐

### 10.7 新增页面预览规范

每个新页面都应提供 Previewer 预览能力，参照 FIX_BY_LIU.md 的标准模板：

```typescript
@Entry
struct XXXPagePreview {
  @Provide('pageInfos') pageInfos: NavPathStack = new NavPathStack();

  build() {
    Column() {
      XXXPage();
    }
    .width('100%')
    .height('100%');
  }
}
```

---

## 十一、开发优先级

### 第零阶段：搭架子（框架先行）
> 目标：把所有模块、接口、组件骨架搭好，结构跑通，用 Mock 数据占位

- [ ] 创建独立 aiAssistant 模块（模块配置 + build-profile）
- [ ] 定义消息类型枚举（MessageType，含 ERROR 类型）和消息接口（ChatMessage，含 errorCode/retryable/metadata）
- [ ] 定义用户行为画像接口（UserBehaviorProfile，含冷启动策略）
- [ ] 定义错误状态接口（ErrorState）和降级策略常量
- [ ] ChatService 单例骨架（sendMessage / getHistory / handleError 空方法）
- [ ] BehaviorTracker 埋点工具骨架（track 空方法，预留节流接口）
- [ ] AIChatPage 页面骨架（NavDestination + 路由注册）
- [ ] ChatMessageBubble 组件骨架（支持文本和 ERROR 消息占位）
- [ ] ChatInputBar 组件骨架（输入框 + 发送按钮占位）
- [ ] 页面 Previewer 预览组件（XXXPagePreview 包装）
- [ ] 隐私政策弹窗组件骨架（首次进入显示）

### 第一阶段（MVP）
- [ ] 聊天界面样式完善（气泡、头像、滚动列表）
- [ ] 文本消息收发（Mock 数据模拟 AI 回复）
- [ ] 快捷问题按钮组件（固定示例数据）
- [ ] AI 回复加载动画（三点跳动）
- [ ] 车型卡片消息组件
- [ ] 接入火山方舟 API 真实调用（含降级逻辑）
- [ ] Prompt 工程设计落地（System Prompt、Token 限制、上下文截断）
- [ ] 幻觉防护（车型名称/参数与本地数据库交叉验证）
- [ ] BehaviorTracker 接入 Preferences 持久化
- [ ] buyingCar 详情页收藏/浏览埋点落地（收藏区分添加/取消，停留时长计时器）
- [ ] 用户画像基础计算逻辑（浏览/收藏权重，30天衰减）
- [ ] 信息完整度计算逻辑（权重分配、阈值判定、兜底策略）
- [ ] 错误处理与异常流程（ERROR 消息展示、重试按钮、降级触发）

### 第二阶段（增强）
- [ ] 选项消息（五问式引导）
- [ ] 自然语言为主 + 五问式为辅的混合模式
- [ ] 多轮对话上下文（sessionId 管理、context 拼接）
- [ ] 对比表格消息
- [ ] 用户画像影响推荐权重（冷启动策略落地）
- [ ] explore / shoppingMall 埋点（列表浏览曝光、商品收藏）
- [ ] 键盘适配与软键盘交互（avoidKeyboard、自动滚动）
- [ ] 快捷问题动态生成（基于用户画像、热门搜索）
- [ ] 语音输入（需真机验证）
- [ ] 图片上传（需真机验证）

### 第三阶段（优化）
- [ ] 消息流式输出（逐字显示，SSE 解析）
- [ ] 消息长按复制/转发
- [ ] 推荐结果收藏
- [ ] 历史对话记录
- [ ] 消息时间戳分组
- [ ] 画像数据可视化（用户偏好报告）
- [ ] 个性化推荐精度优化
- [ ] 后台/锁屏行为处理（请求取消、状态保存）
- [ ] 性能优化（消息列表虚拟滚动、图片缓存）

---

## 十二、与现有功能对比

| 维度 | 五问式问卷 | AI 智能助手（新） |
|------|-----------|------------------|
| 交互方式 | 固定步骤选择 | 自然语言对话 + 五问辅助 |
| 灵活性 | 低，只能选预设选项 | 高，自由表达需求 |
| 学习成本 | 低，一看就会 | 低，像聊天一样 |
| 推荐精度 | 受选项限制 | 更精准，结合行为画像 |
| 开发难度 | 简单 | 中等偏难 |
| 用户体验 | 工具感强 | 更智能、更友好 |
| 模块位置 | buyingCar 内 | 独立 aiAssistant 模块 |
| 行为数据 | 不使用 | 全 App 行为驱动推荐 |

### 12.1 aiAssistant 与 buyingCar 共存策略

**定位**：
- **buyingCar 五问式问卷**：降级为快速入口/轻量版，适合时间紧张、需求明确的用户
- **aiAssistant AI 智能助手**：增强版独立入口，适合需求复杂、希望自然对话的用户

**用户入口**：
- buyingCar 模块内保留五问式问卷入口（"快速选车"）
- 导航栏新增"AI 顾问"Tab，进入 aiAssistant 模块
- 两个入口相互独立，但共享用户画像数据

**数据打通**：
- 用户画像数据（BehaviorTracker）：两个模块共用，实时同步
- 推荐服务（AICarRecommendService）：aiAssistant 复用 buyingCar 的推荐算法
- 对话上下文：两个入口独立管理，不共享对话历史
- 收藏/浏览数据：共用同一套数据源

**迁移策略**：
- 短期：两个入口并存，用户可自由选择
- 中期：根据用户使用数据评估，决定是否将五问式问卷迁移到 aiAssistant 内部作为一种交互模式
- 长期：如果 aiAssistant 完全覆盖五问式的使用场景，可考虑下线五问式问卷入口

### 12.2 服务端规划（补充）

当前项目为纯客户端实现，存在以下安全风险：
- API Key 硬编码在客户端代码中（FIX_BY_LIH.md 已暴露 `ark-5a920e71-...`）
- 对话历史仅本地存储，无法跨设备同步

**服务端需求**：
| 需求 | 说明 | 优先级 |
|------|------|--------|
| API Key 代理 | 客户端调用后端 API，后端再调用火山方舟 | ⭐⭐⭐⭐⭐ |
| 对话历史存储 | 对话记录存储在服务端，支持多设备同步 | ⭐⭐⭐ |
| 用户画像同步 | 画像数据云端存储，换设备不丢失 | ⭐⭐⭐ |
| 消息推送 | AI 回复推送通知（App 后台时） | ⭐⭐ |
| 用户鉴权 | 用户登录/注册系统 | ⭐⭐⭐ |

**技术选型（待确定）**：
- 后端框架：Node.js / FastAPI
- 数据库：MySQL / MongoDB
- 鉴权：JWT / OAuth2

> **注**：当前阶段以客户端实现为主，服务端规划作为后续迭代方向。API Key 安全性问题在客户端阶段通过混淆和代码保护缓解。

---

## 十三、测试策略

### 13.1 单元测试

**测试框架**：HarmonyOS 内置单元测试框架（`@ohos.test.arkui`）

**测试模块**：
| 模块 | 测试内容 | 测试方法 |
|------|----------|----------|
| **推荐算法** | 五维评分计算、权重分配、降级策略 | 输入模拟数据，验证输出结果 |
| **信息完整度计算** | 各维度权重、阈值判定、兜底逻辑 | 边界值测试、异常输入测试 |
| **行为画像计算** | 权重更新、衰减算法、冷启动策略 | 时间序列数据测试 |
| **Prompt 构建** | Token 计数、上下文截断、格式校验 | 长对话场景测试 |
| **错误处理** | 网络异常、API 超时、解析失败 | Mock 异常注入测试 |

### 13.2 集成测试

**测试场景**：
- 完整对话流程（自然语言路径）
- 五问式引导流程
- 降级/恢复流程
- 用户画像影响推荐结果
- 流式响应更新 UI

### 13.3 AI 回复质量评估

**主观评价指标**：
| 指标 | 评分标准 |
|------|----------|
| **准确性** | 推荐车型参数是否正确 |
| **相关性** | 推荐结果是否匹配用户需求 |
| **完整性** | 是否覆盖用户所有需求点 |
| **自然度** | 回复是否像人类对话 |
| **无幻觉** | 是否存在虚构数据 |

**测试方法**：
- 真人评测（5-10 名测试用户）
- 对比 baseline（五问式问卷推荐结果）

### 13.4 真机兼容性测试

**设备矩阵**：
| 设备类型 | 分辨率 | 系统版本 |
|----------|--------|----------|
| 手机 | 1080×2340 | HarmonyOS 4.0+ |
| 平板 | 2000×2400 | HarmonyOS 4.0+ |
| 折叠屏 | 2272×1920 | HarmonyOS 4.0+ |

### 13.5 各阶段测试检查点

| 阶段 | 检查点 | 负责人 |
|------|--------|--------|
| 第零阶段 | 模块结构正确、路由注册成功、Mock 数据可运行 | 开发人员 |
| 第一阶段 | 聊天界面交互正常、API 调用成功、埋点数据正确 | 开发人员 |
| 第二阶段 | 五问式引导完整、多轮对话上下文正确、画像权重生效 | 开发人员 |
| 第三阶段 | 流式响应流畅、性能达标、兼容性测试通过 | QA |

---

## 十四、键盘适配与软键盘交互

### 14.1 键盘适配策略

```
软键盘弹出
    │
    ├─ 消息列表自动滚动到底部（确保最新消息可见）
    │
    ├─ 输入框跟随键盘上移（使用 avoidKeyboard 属性）
    │
    └─ 车型卡片等较大组件弹出时自动收起键盘
```

### 14.2 具体实现

| 场景 | 实现方式 |
|------|----------|
| 键盘弹出时列表滚动 | `List` 组件监听键盘高度变化，调用 `scrollToIndex()` |
| 输入框跟随键盘 | `Scroll` 组件设置 `avoidKeyboard(true)` |
| 点击车型卡片收起键盘 | 点击事件中调用 `hideKeyboard()` |
| 旋转屏幕重置键盘 | `aboutToResize()` 生命周期中处理 |

---

## 十五、后台/锁屏行为

### 15.1 App 切到后台

| 场景 | 处理方式 |
|------|----------|
| AI 请求进行中 | 取消请求（避免资源浪费） |
| 收到 AI 回复 | 不推送通知（当前阶段） |
| 用户主动切后台 | 保存对话状态到本地 |

### 15.2 App 被系统杀死

| 场景 | 处理方式 |
|------|----------|
| 未完成的对话 | 重新进入时恢复最近一次对话状态 |
| 用户输入未发送 | 丢弃未发送的输入 |

---

## 十六、依赖清单

### 16.1 新增依赖

| 依赖 | 用途 | 来源 |
|------|------|------|
| **@ohos.net.http** | HTTP 请求（AI API 调用） | HarmonyOS 系统 API |
| **@ohos.data.preferences** | 本地存储（用户画像、对话历史） | HarmonyOS 系统 API |
| **@ohos.multimedia.audio** | 语音输入（后续阶段） | HarmonyOS 系统 API |
| **@ohos.multimedia.image** | 图片上传（后续阶段） | HarmonyOS 系统 API |

### 16.2 复用现有依赖

| 依赖 | 用途 | 来源 |
|------|------|------|
| **AICarRecommendService** | 推荐算法、车型数据 | buyingCar 模块 |
| **PreferencesUtil** | 本地存储工具 | common 模块 |
| **LoggerUtil** | 日志记录 | common 模块 |
| **CarInfo** | 车型数据模型 | buyingCar 模块 |

### 16.3 缺失依赖（需调研）

| 依赖 | 用途 | 调研内容 |
|------|------|----------|
| **Markdown 渲染** | AI 回复格式化显示 | HarmonyOS 是否有内置组件 |
| **SSE 解析** | 流式响应处理 | `@ohos.net.http` 是否支持流式读取 |
| **图片缓存** | 车型图片懒加载 | 是否需要自定义缓存机制 |

---

## 十七、快捷问题生成策略

### 17.1 第零阶段：固定示例数据

```typescript
const quickQuestions = [
  '15万纯电SUV',
  '续航最长的车型',
  '家庭用车推荐',
  '性价比高的车型'
];
```

### 17.2 第二阶段：动态生成

**生成策略**：
| 策略 | 触发条件 | 优先级 |
|------|----------|--------|
| **基于用户画像** | 用户有足够行为数据 | ⭐⭐⭐⭐⭐ |
| **基于热门搜索** | App 内搜索热度统计 | ⭐⭐⭐ |
| **基于时间/季节** | 特定时间段（如春节前推荐家用车） | ⭐⭐ |
| **基于用户输入上下文** | 当前对话中用户提到的关键词 | ⭐⭐⭐⭐ |

**实现方式**：
- 从用户画像中提取高置信度的偏好维度
- 组合成自然语言问题（如"预算20万左右的SUV"）
- 去重后展示 3-4 个快捷问题
